// Navigation is handled entirely by useDetourGate in the root layout.
// This screen exists only as the required index for the (app) group and
// renders nothing — the gate replaces it before the splash hides.
export default function AppIndex() {
  return null;
}
