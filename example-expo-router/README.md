# Detour Expo Router Example

This example app uses Expo + expo-router to showcase @swmansion/react-native-detour.

- App entry: `expo-router/entry`
- Routes: `app/`
- Detour integration: `app/_layout.tsx`
